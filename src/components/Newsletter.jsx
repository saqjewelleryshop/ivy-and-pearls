import React,{useState} from 'react';import {api} from '../lib/api';import {Link} from 'react-router-dom';
export default function Newsletter(){const [email,setEmail]=useState(''),[consent,setConsent]=useState(false),[state,setState]=useState('');
 async function submit(e){e.preventDefault();setState('Sending…');try{await api('/newsletter',{method:'POST',body:JSON.stringify({email,consent})});setState('Welcome to the list.');setEmail('');setConsent(false)}catch(e){setState(e.message)}}
 return <section className="newsletter"><div className="container newsletter__grid"><div><p className="eyebrow">The Ivy &amp; Pearls List</p><h2>Notes worth <em>receiving.</em></h2><p>New pieces, considered guidance and occasional notes from Ivy &amp; Pearls — delivered quietly to your inbox.</p></div>
 <div><p className="newsletter__lead">A considered edit of new arrivals, styling notes and stories from Ivy &amp; Pearls.</p><form onSubmit={submit}><div className="newsletter__field"><label className="sr-only" htmlFor="newsletter-email">Email address</label><input id="newsletter-email" type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address" required/><button>Join the list ↗</button></div>
 <label className="newsletter__consent"><input type="checkbox" checked={consent} onChange={e=>setConsent(e.target.checked)} required/><span>I agree to receive Ivy &amp; Pearls updates. <Link to="/privacy-policy/">Privacy policy</Link>.</span></label></form><small aria-live="polite">{state||'Considered updates only. Unsubscribe whenever you wish.'}</small></div></div></section>
}
