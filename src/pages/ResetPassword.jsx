import React,{useState} from 'react';import {useNavigate} from 'react-router-dom';import Seo from '../components/Seo';import {useAuth} from '../context/AuthContext';
export default function ResetPassword(){const {supabase}=useAuth();const nav=useNavigate();const [password,setPassword]=useState(''),[state,setState]=useState('');
 async function submit(e){e.preventDefault();if(!supabase)return setState('Account service is not configured yet.');const {error}=await supabase.auth.updateUser({password});if(error)setState(error.message);else nav('/account/');}
 return <><Seo title="Choose a new password" description="Choose a new Ivy & Pearls account password." path="/reset-password/" noindex/><section className="auth-page"><div className="auth-card"><h1>Choose a new <em>password.</em></h1><form onSubmit={submit}><label>New password<input type="password" minLength="8" required value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="button button--dark">Update password</button><p>{state}</p></form></div></section></>}
