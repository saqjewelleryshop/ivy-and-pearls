import React,{useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import Seo from '../components/Seo';import {useAuth} from '../context/AuthContext';
export default function Login(){const {supabase}=useAuth();const nav=useNavigate();const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[state,setState]=useState('');
 async function submit(e){e.preventDefault();if(!supabase){setState('Account service is not configured yet.');return;}setState('Signing in…');const {error}=await supabase.auth.signInWithPassword({email,password});if(error)setState(error.message);else nav('/account/');}
 return <><Seo title="Sign in" description="Sign in to your Ivy & Pearls account." path="/login/" noindex/><section className="auth-page"><div className="auth-card"><p className="eyebrow">Your account</p><h1>Welcome <em>back.</em></h1><form onSubmit={submit}><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" required value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="button button--dark">Sign in</button><p aria-live="polite">{state}</p></form><p><Link to="/forgot-password/">Forgotten your password?</Link></p><p>New to Ivy &amp; Pearls? <Link to="/register/">Create an account</Link>.</p></div></section></>}
