import React,{useState} from 'react';import Seo from '../components/Seo';import {useAuth} from '../context/AuthContext';
export default function ForgotPassword(){const {supabase}=useAuth();const [email,setEmail]=useState(''),[state,setState]=useState('');
 async function submit(e){e.preventDefault();if(!supabase){setState('Account service is not configured yet.');return;}const {error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${location.origin}/reset-password/`});setState(error?error.message:'If that email belongs to an account, a reset link has been sent.');}
 return <><Seo title="Reset password" description="Reset your Ivy & Pearls account password." path="/forgot-password/" noindex/><section className="auth-page"><div className="auth-card"><p className="eyebrow">Account access</p><h1>Reset your <em>password.</em></h1><p>Enter your email and we’ll send a secure reset link.</p><form onSubmit={submit}><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><button className="button button--dark">Send reset link</button><p aria-live="polite">{state}</p></form></div></section></>}
